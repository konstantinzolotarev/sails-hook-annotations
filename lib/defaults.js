'use strict';

module.exports = {

  /**
   * Should hook handle policy annotations
   * @type {Boolean}
   */
  policy: true,

  /** @type {Boolean} should route annotation be loaded */
  route: true
};
